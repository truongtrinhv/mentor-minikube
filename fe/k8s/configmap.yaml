apiVersion: v1
kind: ConfigMap
metadata:
  name: mentorplatform-fe-config
  labels:
    app: mentorplatform-frontend
data:
  # API Service URL - using /api prefix, nginx will proxy to backend
  VITE_API_URL_ROOT: "/api"
